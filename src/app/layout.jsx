import { Providers } from '@/app/providers'
import { Layout } from '@/components/Layout'

import '@/styles/tailwind.css'

export const metadata = {
  title: {
    template: '%s - Claudia Vanea',
    default:
      'Claudia Vanea - Foundational and Applied AI, Oxford PhD in AI for Health',
  },
  description:
    'I’m Claudia, an AI researcher based in New York City. I recently completed a PhD at Oxford in AI for Health and I am curently exploring what’s next at South Park Commons.',
  alternates: {
    types: {
      'application/rss+xml': `${process.env.NEXT_PUBLIC_SITE_URL}/feed.xml`,
    },
  },
}

export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full antialiased" suppressHydrationWarning>
      <body className="flex h-full bg-zinc-50 dark:bg-black">
        <Providers>
          <div className="flex w-full">
            <Layout>{children}</Layout>
          </div>
        </Providers>
      </body>
    </html>
  )
}
